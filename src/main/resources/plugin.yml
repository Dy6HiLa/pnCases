name: pnCases
version: '${version}'
main: ru.privatenull.pnCases
api-version: '1.16'
authors: [ privatenull ]
softdepend: [LuckPerms, FancyHolograms, DecentHolograms, Vault, PlayerPoints]
commands:
  pncases:
    description: dohcases admin commands
    aliases: [cases]
    usage: /pncases createcase <id> | /pncases machine [case] | /pncases setcase <case> | /pncases delcase <case> | /pncases givekey <player> <key> <amount> | /pncases reload
    permission: pncases.admin
permissions:
  pncases.admin:
    default: op
